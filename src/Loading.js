import React from 'react';

const Loading = (props) => {

    return (
        <>
            <div className="dialog loading">
                Loading...
            </div>
        </>
    );
}

export default Loading;